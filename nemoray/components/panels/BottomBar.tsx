import { BottomBarContent } from "@/components/scenario/BottomBarContent";

/** Bottom bar: event timeline scrubber + scenario selector. */
export function BottomBar() {
  return <BottomBarContent className="h-full p-2" />;
}
