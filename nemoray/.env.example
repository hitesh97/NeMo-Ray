# Copy this file to .env.local and fill in the values.
# .env.local is gitignored and never committed.

# Required — MapTiler API key for map tiles, terrain DEM, and style.
# Free tier covers development usage. Sign up at https://maptiler.com
NEXT_PUBLIC_MAPTILER_KEY=your_maptiler_key_here

# Required — Cesium Ion access token for 3D Tiles streaming.
# Free tier: 10 GB/month. Sign up at https://ion.cesium.com → Access Tokens
NEXT_PUBLIC_CESIUM_ION_TOKEN=your_cesium_ion_token_here

# Required — Google Maps Platform API key with Maps Tiles API enabled.
# Free tier: $200 credit/month (~$0.004–$0.016 per session-hour of 3D Tiles).
# Create at https://console.cloud.google.com → APIs & Services → enable "Map Tiles API"
# Restrict the key to your localhost and production domain.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
