import { WorkspaceSync } from "@/components/shell/WorkspaceSync";

/** Coverage Twin — map-forward; the left rail drives layers, the map shows RF. */
export default function CoveragePage() {
  return <WorkspaceSync workspace="coverage" />;
}
