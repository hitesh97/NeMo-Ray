export type MastSite = { id: string; lat: number; lng: number; active: boolean; signal: number }
export type CoveragePoint = { lat: number; lng: number; signal: number }
export type Proposal = { id: string; lat: number; lng: number; score: number; accepted: boolean }
